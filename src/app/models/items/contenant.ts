export interface Contenant {
    _id: string;         // Identifiant unique
    nom: string;        // Nom du contenant
    contenance: string; // Capacité de contenance (exemple : litres, unités)
    prix: number;       // Prix du contenant
  }