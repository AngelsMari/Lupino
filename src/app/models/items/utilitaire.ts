export interface Utilitaire {
    _id: string;         // Identifiant unique
    nom: string;        // Nom de l'objet utilitaire
    effet: string;      // Effet de l'objet (par exemple : soins, amélioration)
    prix: number;       // Prix de l'objet
}