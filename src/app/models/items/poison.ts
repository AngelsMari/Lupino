export interface Poison {
    _id: string;         // Identifiant unique
    nom: string;        // Nom du poison
    effet: string;      // Effet du poison
    prix: number;       // Prix du poison
  }